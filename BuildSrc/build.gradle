apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.1.0'
    compile 'org.javassist:javassist:3.20.0-GA'
}

repositories {
    jcenter()
    google()
}